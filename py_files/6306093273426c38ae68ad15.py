def _run_playbook(cli_args, vars_dict, ir_workspace, ir_plugin):
	"""
	Runs ansible cli with vars dict

:param vars_dict: dict, Will be passed as Ansible extra-vars
:param cli_args: the list  of command line arguments
:param ir_workspace: An Infrared Workspace object represents the active
 workspace
:param ir_plugin: An InfraredPlugin object of the current plugin
:return: ansible results
	"""
	# Get the playbook name from the cli args
	playbook_name = cli_args[0]
	if not playbook_name:
		raise AnsibleError("No playbook specified")

	# Get the inventory file path from the cli args
	inventory_file_path = cli_args[1]

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory file path from the cli args
	# inventory_file_path = os.path.join(ir_workspace.get_inventory_dir(), inventory_file_path)

	# Get the inventory