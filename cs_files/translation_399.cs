1. Open the source language file (Java in this case) and identify PersonTrackingConfigTrackingJobTrackingStatus = "Pending";
personTrackingObject = new PersonTrackingObject();
}
personTrackingObject.StartNewFrame();
byte[] thumbnail = imageThumbnail = thumbnailData;
bytesUsed.addAndGet(thumbnail.length);
}